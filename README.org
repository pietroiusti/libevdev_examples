Examples based on libevdev's docs.

The examples require libevdev installed.

Compile with
#+begin_src
gcc `pkg-config --cflags libevdev` example_name.c `pkg-config --libs libevdev` -o example_name
#+end_src
